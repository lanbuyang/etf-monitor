name: 更新 ETF KD 監控頁面

on:
  schedule:
    # 台灣時間每 2 小時：09:30、11:30、13:30、15:30 = UTC 01:30、03:30、05:30、07:30，週一至週五
    - cron: "30 1 * * 1-5"
    - cron: "30 3 * * 1-5"
    - cron: "30 5 * * 1-5"
    - cron: "30 7 * * 1-5"
  workflow_dispatch:   # 允許手動從 Actions 頁面觸發

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate index.html
        run: python generate.py

      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet || git commit -m "chore: 自動更新 ETF KD 數據 $(date +'%Y-%m-%d %H:%M UTC')"
          git push
